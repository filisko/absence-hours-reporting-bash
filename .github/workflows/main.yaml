name: test matrix
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ macos-latest, ubuntu-latest ]
        include:
        - os: macos-latest
        - os: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: 'install bashunit'
      run: |
        mkdir lib
        curl -s https://bashunit.typeddevs.com/install.sh | bash
        chmod +x lib/bashunit  # Ensure script is executable

    - name: 'run tests'
      run: |
        $PWD/lib/bashunit tests || exit 1
